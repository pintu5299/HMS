
<div class="col-lg-12">
<div class="card rounded-0 mt-5 mb-5">
  <div class="card-body">
    <h2 class="text-center" style="color: blue;">Patient's Name</h2>
  </div>
</div>
<%= link_to "Create New Patient", new_patient_path, class: "btn btn-primary btn-lg mb-4 rounded-0" %>
<table class="table table-bordered">
    <thead class="thead-dark">
      <tr class="colo">
        <th style="color:blue;">Patient Id</th>
        <th style="color:blue;">name</th>
        <th style="color:blue;">age</th>
        <th style="color:blue;">guardian_name</th>
        <th style="color:blue;">mobile</th>
        <th style="color:blue;">address</th>
        <th style="color:blue;">Action</th>
        <th style="color:blue;">Action</th>
        <th style="color:blue;">Action</th>
      </tr>
    </thead> 
    <tbody> 
      <% @patients.each do |patient|%>  
        <tr>
            <td ><%= patient.id %></td>
            <td ><%= patient.name %></td>
            <td ><%= patient.age %></td>
            <td ><%= patient.guardian_name %></td>
            <td ><%= patient.mobile %></td>
            <td><%=  patient.address %></td>

            <td>
              <%= link_to "edit", edit_patient_path(patient), class:"btn btn-primary"%> 
            </td>
            <td>
              <%= link_to "show", patient_path(patient), class:"btn btn-info"%> 
            </td>
            <td>
              <%= link_to "Delete", patient ,data:{turbo_method: :delete,turbo_confirm: :"Are you sure to delete this Doctor?"}, class:"btn btn-danger" %>
            </td>
          </tr>
        </th>
      <% end %> 
    </tbody>
</table>


