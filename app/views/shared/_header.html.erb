
<nav class="navbar navbar-expand-lg navbar-primary bg-primary sticky-top">
  <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
  <%= link_to 'Hospital Management' , root_path, class:"navbar-brand text-white"  %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span> 
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <div class="navbar-nav mr-auto"> 
      <div class="nav-item">  
        <%= link_to "Department", homes_about_path, class:"nav-link text-white" %>
      </div> 
    
            <div class="nav-item">
        <%= link_to "Doctor", doctors_path, class:"btn btn-primary" %>
      </div>
           
            <div class="nav-item"> 
        <%= link_to "Patient", patients_path, class:"nav-link text-white" %>
      </div>
    
       <div class="nav-item">
        <%= link_to "Add New Doctor",new_doctor_path, class:"nav-link text-white" %>
      </div>

        <div class="text-end">  
        <% if user_signed_in? %>
        Are you admin? <%= current_user.has_role? :admin %>
        <%= current_user.roles.pluck(:name) %>
        <%= link_to current_user.email, edit_user_registration_path, class: "btn btn-primary" %>
        <%= link_to "sign out", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: :" if you logout, your current session will be destroyed.Are you sure you want to log out ?"} ,class:"btn btn-danger"%>
         <% else %>
         <%= link_to "Login",  new_user_session_path, class:"btn btn-warning"%>
         <% end %>
   
    </div>
  </div>
</nav>